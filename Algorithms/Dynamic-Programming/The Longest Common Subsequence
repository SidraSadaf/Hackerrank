#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int m,n;
    cin>>m>>n;
    vector<int> a(m);
    vector<int> b(n);
    for(int i=0;i<m;++i){
        cin>>a[i];
    }
    for(int i=0;i<n;++i){
        cin>>b[i];
    }
    int l[m+1][n+1];
    for(int i=0;i<=m;++i){
        l[i][0]=0;
    }
    for(int i=0;i<=n;++i){
        l[0][i]=0;
    }
    for(int i=1;i<=m;++i){
        for(int j=1;j<=n;++j){
            if(a[i-1]==b[j-1])
                l[i][j]=1+l[i-1][j-1];
            else
                l[i][j]=max(l[i][j-1],l[i-1][j]);
        }
    }
    int index=l[m][n];
    vector<int> v(index+1);
    int i=m,j=n;
    int p=index-1;
    while(p>=0){
        if(a[i-1]==b[j-1]){
            v[p]=a[i-1];
            i--;
            j--;
            p--;
        }
        else if(l[i-1][j]>l[i][j-1])
            i--;
        else
            j--;
    }
    for(int i=0;i<index;++i)
        cout<<v[i]<<" ";
    return 0;
}
